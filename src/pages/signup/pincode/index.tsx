import { NextPage } from 'next';
import Head from 'next/head';
import { Fragment } from 'react';

const Page: NextPage = () => (
  <Fragment>
    <Head>
      <title>pincode</title>
    </Head>
    <main>
      <p>6桁のpincode入力画面</p>
    </main>
  </Fragment>
);

export default Page;
