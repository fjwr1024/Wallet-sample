import { NextPage } from 'next';
import Head from 'next/head';
import { Fragment } from 'react';

const Page: NextPage = () => (
  <Fragment>
    <Head>
      <title>Signup</title>
    </Head>
    <main>
      <p>会員登録画面</p>
    </main>
  </Fragment>
);

export default Page;
